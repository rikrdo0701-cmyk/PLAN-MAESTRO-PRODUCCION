# Rules registry

Registro de todas las reglas con ID estable `RULE-<DOMINIO>-NNN`. Cada regla tiene una sola
fuente canónica y una clasificación: **DOCUMENTADA**, **IMPLEMENTADA**, **INFERIDA** o
**AMBIGUA** (ver `docs/REGLAS.md`). La versión machine-readable vive en
`.project-memory/rules.json`.

## Gobernanza

| ID | Nombre | Estado | Fuente |
|---|---|---|---|
| `RULE-GOV-001` | Project Memory first | DOCUMENTADA | ADR-0001 |
| `RULE-GOV-002` | Single writer | DOCUMENTADA | ADR-0001 |
| `RULE-GOV-003` | Document discovered knowledge | DOCUMENTADA | ADR-0001 |
| `RULE-GIT-001` | Private independent remote | DOCUMENTADA | ADR-0001 |
| `RULE-GOV-004` | Dry-run == ejecución real (sin fallbacks que la web no hace) y snapshots en SNAPSHOT_PAYLOADS (consolidada 2026-09-05) | IMPLEMENTADA | `planner-core.js`, `02-storage.js` |
| `RULE-GOV-005` | GitHub Pages es el punto de acceso público a la app | ACTIVA | `deploy-pages.yml` |
| `RULE-GOV-006` | Sin estado de demostración en el arranque público; datos reales se autoaplican | IMPLEMENTADA | `scripts/build-appscript.mjs` |
| `RULE-GOV-007` | El arranque real es `performance-client.js`; parches del boot vía `scripts/build-appscript.mjs` | IMPLEMENTADA | `scripts/build-appscript.mjs` |
| `RULE-GOV-008` | Código inyectado por el build no usa helpers internos de `planner-core` | IMPLEMENTADA | `scripts/build-appscript.mjs` |
| `RULE-GOV-013` | Recarga por conflicto (`CONFLICT_REVISION`) conserva altas locales y ediciones de `CONFIGURACION_OT` mediante tombstones reversos | IMPLEMENTADA | FIX 2026-09-06, `performance-client.js`, `app.js` |

## Arranque (boot / carga)

| ID | Nombre | Estado | Fuente |
|---|---|---|---|
| `RULE-LOAD-001` | Capa 1: hidratar caché local al boot (render instantáneo para recurrentes) | IMPLEMENTADA | plan 2026-09-01 |
| `RULE-LOAD-002` | Capa 2: rescate rápido del Borrador en paralelo; `getAppState` autoritativo después | IMPLEMENTADA | plan 2026-09-01 |
| `RULE-LOAD-003` | El caché local se puebla tras cada carga autoritativa exitosa | IMPLEMENTADA | plan 2026-09-01 |
| `RULE-LOAD-004` | No abrir el modal de "última modificación" si el contenido local vino del servidor | IMPLEMENTADA | plan 2026-09-01 |

Detalle y mediciones: `docs/superpowers/plans/2026-09-01-arranque-con-datos-reales-sin-demo.md`.

## UI / vistas del workspace

| ID | Nombre | Estado | Fuente |
|---|---|---|---|
| `RULE-UI-001` | Cada vista no-plan ocupa todo el workspace (el plan/Gantt queda oculto en Reportes, Cargas, Cuello de botella y Config) | IMPLEMENTADA | `src/web/planning/styles.css`, commit `34c5802` |
| `RULE-UI-003` | Botones Completar/Reabrir del panel de detalle de OT se re-habilitan cuando termina el guardado (incluidos en `planStatusButtons`; antes quedaban `disabled` para siempre tras togglear desde el detalle) | IMPLEMENTADA | `app.js` (`planStatusButtons`), `tests/performance-client-calls.test.mjs` |
| `RULE-UI-004` | Gantt: reconstruye el DOM solo cuando cambia la estructura; abrir el detalle de OT actualiza solo la selección (memo `renderGanttStructureMemo`) | IMPLEMENTADA | bug 2026-09-06 (abrir detalle de OT tarda), `app.js` (`renderGantt`/`applyGanttSelection`) |
| `RULE-UI-005` | Modal Preparar OT muestra el primer material (MP) de la OT en una sección "Materia prima base" | IMPLEMENTADA | definición de usuario 2026-09-06, `app.js` (`showPlanningRequirements`) |
| `RULE-UI-006` | Abrir detalle de OT no re-descarga de NetSuite si las operaciones ya están locales (`ensureWorkOrderPlanningData(ot, { skipWhenLocal: true })`); precarga idéntica; sync/smart-sync/sel. Backlog/manual conservan fetch | IMPLEMENTADA | bug usuario 2026-09-07 (clic tarjeta tarda), `app.js` (`ensureWorkOrderPlanningData`/`loadSelectedJobDetailOperations`/`flushPlanningWorkOrderPrefetchQueue`) |
| `RULE-UI-007` | Cerrar detalle de OT no reconstruye la UI global: `closeSelectedJobDetail()` limpia selección, oculta panel, quita `.focused` y desmarca Gantt (memo) sin `render()` | IMPLEMENTADA | bug usuario 2026-09-07 (cerrar detalle tarda), `app.js` (`closeSelectedJobDetail`) |
| `RULE-PUB-002` | Publicar plan arma payload sin doble clonado: `persistableState()` sin `deepClone` + `operations: currentPlanOperations()` (el servidor reescribe hojas, limitación conocida) | IMPLEMENTADA | bug usuario 2026-09-07 (Publicar se queda en 40%), `app.js` (`publishCurrentPlan`/`persistableState`) |
| `RULE-UI-009` | Gantt sin costo O(n²): `isOtScheduled` cachea la intersección `selected ∩ scheduled` en `Set` por identidad de arrays (O(1)); `getGanttGroups()` cacheKey barato (filtro solo en cache miss); `workWindowMinutes()` una vez por render, pasado a `createGanttBar`/`createGanttSummaryBar` | IMPLEMENTADA | bug usuario 2026-09-08 (93.6 s abriendo detalle OT 2131, clicks 167-512 ms), diagnóstico Playwright local 2026-09-08, `planner-core.js` (`isOtScheduled`), `app.js` (`getGanttGroups`/`createGanttBar`) |
| `RULE-UI-010` | Gantt reutiliza el DOM por firma estructural (`ganttStructureDomCache`, clave `view|groupsSignature|windowStart|dayWidth|horizonDays`, cap 4 vistas): el cambio de vista OT/Operador/CT deja de reconstruir ~1578 filas/~1400 barras en cada switch (~500 ms -> ~80 ms reuso; primera construcción intacta); DOM detached conserva listeners; `renderGantt` sigue siendo el único writer de `els.ganttCanvas` | IMPLEMENTADA | auditoría de rendimiento interactiva 2026-09-11 (producción: switches 354-474 ms y ~500 ms repetidos), FIX `app.js` (`renderGantt`) 2026-09-11, verificación Playwright local `site/` :8787 (500 ms -> 76-105 ms) |
| `RULE-UI-011` | Los paneles Backlog y Planeado / Por planear exportan listas OT a Excel (.xlsx) sin librerías: botones "Exportar" (`exportBacklogXlsxBtn`/`exportQueueXlsxBtn`); Backlog exporta `ot, articulo, descripcion, cantidad, fecha entrega` (mismos filtros que el render), Planeado exporta `ot, articulo, descripcion, cantidad, fecha entrega, maquina, herramental, tipo` (OTs en orden de cola). Escritor XLSX mínimo en JS puro (`buildXlsxBytes`): ZIP método STORE + CRC-32 (`buildZipBytes`/`crc32`) y hoja SpreadsheetML (`buildSheetXml`, celdas `inlineStr`) | IMPLEMENTADA | definición de usuario 2026-09-19, `index.template.html` (botones), `app.js` (`exportBacklogXlsx`/`exportQueueXlsx`/`buildXlsxBytes`), `styles.css` |
| `RULE-UI-013` | Completar una operación persiste POR CASCADA todas las anteriores de la secuencia de la OT en UNA sola llamada atómica `saveOperationPlanStatus(statuses[])`: `completePriorSequenceOperations` devuelve las claves afectadas con su estado previo; `persistOptimisticPlanStatus` envía `{ revision, status, statuses }` y en fallo revierte marcada + cascada; el server de `15-performance-service.js` acepta `statuses[]` (un lock, una escritura de ESTADOS_OPERACION_PLAN) con compatibilidad `payload.status`; el render dirigido actualiza cada fila afectada (`renderPlanStatusRow`) y el candado de la tarjeta de cola (`updateQueueLockCard`, patrón RULE-UI-012) sin render global | IMPLEMENTADA | bug reportado 2026-09-19 (al reabrir el detalle solo quedaba completada la marcada), FIX `app.js` + `15-performance-service.js` commit `d203da6`, `tests/performance-client-calls.test.mjs` + `tests/storage-state.test.mjs` |

Patrón: `.workspace[data-view="VISTA"] > :not(.topbar):not(PANEL):not(.toast):not(.planning-dialog) { display: none; }`, espejo de lo que `src/web/inspection/inspection.css` hace con `data-view="inspection"`.

## Rendimiento / payloads (PERF)

| ID | Nombre | Estado | Fuente |
|---|---|---|---|
| `RULE-PERF-001` | Los payloads de publicaci\u00f3n/guardado excluyen `machineToolHistory` (dato derivado del servidor de `PLANES_HISTORICOS`, no escrito desde el payload): publish baja de 2,077,555 B a ~1.51 MB; se conservan `operations`/`workOrders`/`materials`; el snapshot ya no persiste la copia de `machineToolHistory` que viajaba via `persistableState` y al restaurar se re-deriva por `PP_readState_`. FIX 2026-09-12: la exclusi\u00f3n sube a `persistableState()` (`delete persisted.machineToolHistory;`), con lo que `createAppSheetPayload` deja de deep-clonar ~565 KB por autosave y la f\u00f3rmula `JSON.stringify({...persistableState(), operations: currentPlanOperations()}).length` baja de 2,068,735 B a ~1.5 MB (< 2 MB); los `delete` en `publishCurrentPlan`/`createAppSheetPayload` quedan como no-op defensivos que los tests exigen | IMPLEMENTADA | PLAYWRIGHT producci\u00f3n 2026-09-10/2026-09-12 (`probe-perf-prod-2026-09-10.mjs`, `probe-perf-maintenance-2026-09-12.mjs`), `02-storage.js:227/630/1270-1292`, FIX 2026-09-10 + 2026-09-12 `app.js` (`persistableState`/`publishCurrentPlan`/`createAppSheetPayload`), `tests/build.test.mjs` |
| `RULE-PERF-002` | Cuello de botella (saturation) usa `operatorLoadsSourceMemoized` compartido con Cargas (misma fuente `loadOperationsForMode` + guards): render de la vista baja de ~1.6-1.9 s (reproducido 1650-1875 ms en producción) a ~0 ms tras el primer render (medido 35/19-38 ms); `operatorLoadsForOperations` sigue intacta y los reportes (~1.15 s) quedaron resueltos con memo dedicada (RULE-PERF-003) | IMPLEMENTADA | auditoría de rendimiento interactiva 2026-09-11 (`[planning perf] saturation-render` 1875/1678/1650 ms), FIX `app.js` (`renderSaturation` :4493) 2026-09-11, verificación Playwright local `site/` :8787 |
| `RULE-PERF-003` | Reportes usan memo dedicada `reportOperatorLoadsSourceMemoized` (firma de contenido `reportLoadsSignature` por op + guardas de identidad de state/`reportSnapshot`/`planStateMutationVersion`, path rápido `memo.reportOps === reportOperations`, caché por semana `reportOperatorLoadsWeekCache` Map cap 4): `renderReports` de ~1.15 s en CADA render a ~250 ms cold / ~216 ms warm; alternar semanas pasa de ~900 ms en cada dirección a 914 ms (primer compute de semana nueva) y ~215 ms al volver vía caché; se usa en `weeklyExecutiveSummary` y `renderReportOperatorLoads` (dos llamadas antes ~560-660 ms c/u); NO comparte la memo de Cargas/Saturation (fuente distinta); `operatorLoadsForOperations` intacta | IMPLEMENTADA | auditoría de rendimiento interactiva 2026-09-11 (`perf_probe_reports1/2.py` desglose renderWeekReport 1131 ms), FIX `app.js` 2026-09-11 (`weeklyExecutiveSummary` :6784, `renderReportOperatorLoads` :6749), verificación Playwright local `site/` :8787 (perf_probe_reports3-7.py, 503/503 y build 2.41.1 OK) |
| `RULE-PERF-004` | Flujo "Generar plan" optimizado con caches de funciones puras en `planner-core.js`: profiling CDP mostró ~80% de CPU en normalización de strings (`normalizeKey` 45.5%, `resolveOperationCt` 26.5%, `capabilityForOperation` 6.1%). FIX de equivalencia exacta: (1) `normalizeKey` memoizada (Map cap 200000), (2) `resolveOperationCt` cacheada por `state.__ctResolution` validado por identidad de catálogos (mismo orden de precedencia y first-wins), (3) `capabilityForOperation` con WeakMap por op, (4) `isSubcontractOperation` con WeakMap por op, (5) `checkPlanningBudget` con muestreo de reloj `BUDGET_CLOCK_SAMPLE=4096` solo cuando `completeAll=true` (nunca aborta; para `completeAll!==true` comportamiento exacto). Determinista y sin cambio de decisión: dry-run `window.runPlanningPerformanceDryRun` de 89.5 s a 15.6 s (scheduler 88.7 s -> 15.3 s, ~5.8×) en la misma carga (140 OTs, 1.1M slot probes, 7.3M toolCatalogScans, 6.1M budget checks) con plan completo 1403/1403 y 0 sin hueco; estrategias/comparadores/precedencia/calendario/gap-fill intactos | IMPLEMENTADA | escaneo 2026-09-11 `perf_probe_gen1.py` (baseline 89.5 s/88.7 s) + `perf_probe_genprof2/3.py` (profiling CDP), FIX `planner-core.js` 2026-09-11, verificación Playwright local `site/` :8787 (15.6 s, 1403/1403, 0 errores), suite 503/503 y build OK |
| `RULE-PERF-005` | Diagnóstico corregido: producción no incluía la memo local; operaciones vacías en localStorage no prueban ausencia en memoria. Fix acotado: invalidar Map semanal Y memo rápido al cambiar dependencias (capacidad 100→200, carga 50: resultado correcto 25 %). Medición integral reportes/PDF/publicación pendiente; separar preparación cliente, render y backend. | DOCUMENTADA; FIX ACOTADO IMPLEMENTADO | `tests/report-loads-cache.test.mjs`; [orígenes, restricciones y evidencia](../performance/RULE-PERF-005.md) |
| `RULE-PERF-006` | `createAppSheetPayload` arma el payload SIN `deepClone` (patrón RULE-PUB-002 aplicado al guardado automático): cada autosave dejaba de serializar el estado completo DOS veces (`{...deepClone(persistableState(source))}`) antes del envío. FIX 2026-09-14: `persistableState(source)` directo + deletes de markers sobre el objeto nuevo; verificado seguro en los 4 callers (persistPlanSnapshot/persistPlanAutoBackup reparten en `buildDraftSnapshot` que no muta anidados; previewDraftRestore reparte en objeto nuevo; saveAppSheet serializa de inmediato). Ahorro ~39 ms por save (17 ms stringify + 23 ms parse) con 2.4 MB de estado (ej. 2218 ops / 1.88 MB operations). Guard de test actualizado (`const payload = persistableState(source);` y prohibido `...deepClone`). | IMPLEMENTADA | auditoría 2026-09-14 (`perf_payload_breakdown.mjs` producción: persistableState 2,209,619 B), FIX `app.js` (`createAppSheetPayload`:11359), `tests/build.test.mjs`:951, suite 510/510 y build 2.41.1 OK; RULE-PUB-002/RULE-PERF-001 |
| `RULE-PERF-007` | Auditoría de mantenimiento 2026-09-17 (Playwright producción): clicks de tarjetas prioridad sync 14-19.9 ms / total 136-145 ms / 0 POSTs; cierre de detalle sync 3 ms / total 130.9 ms; long tasks en los 10 s pasivos: 1 de 187 ms (<500 ms OK); localStorage 1,766,438 B sobre quota 5 MB, 0 QuotaExceededError; 0 console/page errors. Único criterio superado: payload de publicar 3,073,823 B (>2 MB), causa = volumen legítimo (operations 2,718,005 B en 2283 ops, ~1.19 KB/op, dominado por campos persistidos: columna LOG 262,319 B + descripcion/operador/fechas/horas/planStatus; confirmado contra columnas de `02-storage.js`), NO regresión de código; no se debe "resolver" eliminando campos persistidos. La jank pico de 1226 ms fue long task transitorio de arranque/sync. CAMBIO aplicado (guard preventivo): `optimizedScheduleLocalStorageFlush` serializa una vez y si `serialized.length*2 > 4 MB` (`LOCAL_CACHE_QUOTA_GUARD_BYTES`) omite antes de `setItem` los tombstones/UI no críticos (`_locallyRemovedDraftOts`/`_pendingAddOt`/`_pendingAddOtSnapshot`/`_locallyAddedDraftOts`/`_locallyEditedOtConfigurations`/`expandedOts`); nunca pierde `workOrders`/`materials`/`otConfigurations`/catálogos. Guard en `site/index.html` + 2 tests nuevos en `tests/build.test.mjs`; suite 511/511 y build 2.41.1 OK. | IMPLEMENTADA | medición Playwright producción 2026-09-17 (`.openchamber/probe-perf-prod.mjs`, `probe-payload-ops.mjs`); FIX `performance-client.js` (`trimLocalCachePayload`); tests `build.test.mjs`; RULE-PERF-001/RULE-GOV-001 |
| `RULE-PERF-008` | Auditoría de mantenimiento 2026-09-19 (Playwright producción, `probe-perf-maint-2026-09-18.mjs`): clicks de tarjetas prioridad sync 7.4-34.6 ms / total 135.9-197.3 ms / 0 POSTs; cierre de detalle sync 4.9 ms / total 128.7 ms; long tasks pasivas 52-107 ms (<500 ms OK); **payload de publicar 1,990,931 B (~1.99 MB) bajo 2 MB** (antes 3,073,823 B en 09-17); localStorage 1,756,590 B, 0 QuotaExceededError; todos los umbrales pasan. 1 defecto detectado: console.warn `[maybeRestoreSavedDraftOnBoot] normalizeKey is not defined` — el bundle HEAD usa `normalizeKey()` en app.js sin definirla (es helper interno del factory de planner-core, RULE-GOV-008), por lo que el restore del borrador al boot falla en producción. FIX en working tree pendiente de commit/deploy: global `normalizeKey` en app.js delegando a `window.PlannerCore?.normalizeKey` + export en planner-core; bundle local en `site/index.html`; suite 514/514, build 2.41.1 OK. | IMPLEMENTADA | medición Playwright producción 2026-09-19; `git show HEAD:src/web/planning/app.js` vs working tree; `tests/queue-fastpath.test.mjs`; RULE-GOV-008 |
| `RULE-PERF-009` | Auditoría de mantenimiento 2026-09-21 (Playwright producción, `probe-perf-maint-2026-09-21.mjs`): ready ops=3480, 24 tarjetas prioridad; clicks sync 33.9-83.9 ms / total 164.3-369.2 ms / 0 POSTs; cierre de detalle sync 12.8 ms / total 147.3 ms / 0 POSTs; long tasks pasivas 1 de 477 ms (<500 ms OK, próximo al umbral); **payload de publicar 3,431,870 B (~3.43 MB) SUPERA 2 MB** — causa verificada = volumen legítimo de datos, NO regresión: el plan pasó de 1979 ops (RULE-PERF-008) a 3480 ops a ~833 B/op (menor al ~1.19 KB/op de 09-17); operations 2,897,627 B con campos todos persistidos en OPERACIONES y publishedPlanStatuses 222,480 B = mirror del schema de 23 columnas de ESTADOS_OPERACION_PLAN (PP_operationStatusRow_ 02-storage.js:586) preservado por el servidor; por RULE-PERF-007 NO se eliminan campos persistidos. localStorage 2,140,354 B (~2.04 MB) bajo el guard de 4 MB (trimLocalCachePayload performance-client.js, no se dispara), 0 QuotaExceededError; consoleErrors 0, pageErrors 0. Todos los umbrales pasan salvo payload (volumen legítimo); SIN cambio de código. Recomendación futura: chunking/compresión del payload. | IMPLEMENTADA | medición Playwright producción 2026-09-21 (`.openchamber/probe-perf-maint-2026-09-21.mjs`, log 2026-09-21); verificación persistencia `02-storage.js:586/619/235`; RULE-PERF-007/RULE-PERF-008/RULE-REP-009 |
| `RULE-PERF-010` | Auditoría de mantenimiento 2026-09-22 (Playwright producción, `probe-perf-maint-2026-09-22.mjs`): ready ops=1478, 28 tarjetas prioridad; clicks sync 14.1/11.7/10.6/13.3 ms (max 14.1) / total 142.7/133.3/132.1/135.1 ms / 0 POSTs / 0 long tasks durante clicks; cierre de detalle sync 6.9 ms / total 131.4 ms / 0 POSTs; long tasks pasivas 77/66/69/54/53 ms (max 77 <500 OK). **payload de publicar 1,806,969 B (~1.81 MB) BAJO 2 MB** (operations 1,267,190 B en 1478 ops ~845 B/op; publishedPlanStatuses 228,592 B en 2 origins; workOrders 89,585; materials 77,151; otConfigurations 39,950; articleConfigurations 27,974; operationPlanStatuses 20,429; operationCatalog 10,788). El exceso de RULE-PERF-009 desaparece: el catálogo volvió a 1478-1527 ops (de las 3480 en 09-21), consistente con el dedupe de generaciones RULE-OT-035, con costo por op estable ~845 B/op. localStorage 2,111,968 B (~2.01 MB) bajo el guard de 4 MB (trimLocalCachePayload, no se dispara), 0 QuotaExceededError; consoleErrors 0, pageErrors 0. TODOS los umbrales pasan; SIN cambio de código (la latencia de clicks/cierre sigue en el régimen de memos RULE-UI-004/006/007/009/010/012). Recomendación: vigilar publishedPlanStatuses y chunking solo si el volumen vuelve a crecer. | IMPLEMENTADA | medición Playwright producción 2026-09-22 (`.openchamber/probe-perf-maint-2026-09-22.mjs`, log `%TEMP%/opencode/perf-2026-09-22.log`); RULE-PERF-007/008/009; RULE-OT-035 |
| `RULE-PERF-011` | Auditoría de mantenimiento 2026-09-24 (Playwright producción, `probe-perf-maint-2026-09-24.mjs`, 2 corridas 16:02Z/16:04Z consistentes): ready ops=1436, 30 tarjetas prioridad; clicks sync 14.4/11.7/14.6/13.0 ms (max 14.6) / total 138/144.2/149/146 ms (max 149) / 0 POSTs / 0 long tasks durante clicks; cierre de detalle sync 3.3 ms / total 145.2 ms / 0 POSTs (1 long task de 84 ms durante el cierre, interacción); long tasks pasivas 56/74/58/63/84 ms (max 84 <500 OK); transitorios de boot/sync fuera de pasivo: 840/1326 ms. **payload de publicar 1,827,431 B (~1.74 MB) BAJO 2 MB** (operations 1,241,100 B en 1436 ops ~864 B/op; publishedPlanStatuses 268,845 B [crece vs 228,592 de 09-22]; workOrders 88,137; materials 69,935; otConfigurations 38,909; operationPlanStatuses 34,733; articleConfigurations 28,261; operationCatalog 10,788). localStorage 2,263,372 B (~2.16 MB) bajo el guard de 4 MB (trimLocalCachePayload, no se dispara) y quota 5 MB, 0 QuotaExceededError; consoleErrors 0, pageErrors 0. TODOS los umbrales pasan; SIN cambio de código (régimen de memos RULE-UI-004/006/007/009/010/012/013 + guard anti-quota existente). Observación: ops en tendencia a la baja 3480→1478→1436 (dedupe RULE-OT-035) y publishedPlanStatuses crece por estados acumulados. Suite npm test completa y build OK sin modificaciones. | IMPLEMENTADA | medición Playwright producción 2026-09-24 (`.openchamber/probe-perf-maint-2026-09-24.mjs`, JSON `%TEMP%/opencode/perf-2026-09-24-result.json`); RULE-PERF-007/008/009/010; RULE-OT-035 |

## Reportes

| ID | Nombre | Estado | Fuente |
|---|---|---|---|
| `RULE-REP-001` | Fechas de reportes: día = hoy (operador/ajustador/subcontrato); semana = lunes del plan seleccionado, sin campo "Semana" | IMPLEMENTADA | `app.js`, `index.template.html`, commit `221f4d8` |
| `RULE-REP-002` | Rango de fechas de reportes diarios: muestran operaciones con fecha anterior al día seleccionado y hacia adelante según la casilla de días; no se limitan a la semana del plan (`operationsForDayReport`, `filteredReportRows`, `reportDateRange`) | IMPLEMENTADA | `app.js` (`operationsForDayReport`, `operatorReportSelection`, `adjusterReportSelection`) |
| `RULE-REP-003` | Seguimiento de completado (Completar/Reabrir y columna Estado) SIEMPRE disponible (borrador, último publicado y anteriores); estados por origen con herencia del último publicado al borrador (`reportSourceAllowsOperationTracking` siempre true, `draftViewStatuses`) | IMPLEMENTADA | `app.js` (`reportSourceAllowsOperationTracking`, `planStatusOriginForSource`, `writePlanStatusByOrigin`) |
| `RULE-REP-009` | Estados de operación por origen: columna `ORIGEN` en `ESTADOS_OPERACION_PLAN` (`draft` o snapshotId), buckets separados draft/published en el servidor y cliente, mirror del último publicado al borrador | IMPLEMENTADA | `02-storage.js`, `15-performance-service.js`, `app.js`, `planning-workflow-core.js` |
| `RULE-REP-004` | TC siempre derivado = tiempo de producción ÷ piezas a producir; fuente de reportes solo borrador + último publicado (consolidada 2026-09-05) | IMPLEMENTADA | `08-netsuite.js`, `app.js` |
| `RULE-OT-027` | Mapeo de tiempos de operación NetSuite: headers reales de ruta (`Velocidad de ejecución (minutos/unidad)`, `Tiempo de configuración (minutos)`, `Cantidad de entrada`/`Cantidad completada`) y SIN topes (`setup>20→15` y `rate>10→0.67` eliminados) | IMPLEMENTADA | `08-netsuite.js` |
| `RULE-REP-011` | Corte de fijas = COMPLETADA: completadas anclan y NO reservan capacidad; incompletas de locked se reprograman (supera v2 2026-09-20; CORTE 2026-09-23) | IMPLEMENTADA | decisión 2026-09-22/2026-09-23; `tests/planner-core.test.mjs`; `.project-memory/rules.json` |
| `RULE-REP-013` | Pestaña "Liberación" en Reportes: lista OTs con operación 16OC/39OTD una fila por OT, columnas OT, Artículo, Cantidad (total de la OT: `quantity` de la caché de inspección → `workOrders[].quantity` → pendiente de la op) y Ensamblado (`builtQuantity` de la caché de inspección → del work order), Fecha (inicio de la operación de liberación) y Completar (botón `planStatusActionCell` si `reportSourceAllowsOperationTracking()`); filas ordenadas ascendente por fecha de plan de la operación; exporta `liberacion-final-16oc-39otd.xlsx` (Completar → `Completada`/`Pendiente`); título del panel solo "Liberación" | IMPLEMENTADA | definición usuario 2026-09-23 (columnas Ensamblado/Completar confirmadas); FIX 0 OT con Borrador (SIN_CT); `index.template.html` (`data-tab="release"`), `app.js` (`releaseReportRows`/`renderReleaseReport`/`planStatusButtons`/`exportReleaseXlsx`); tests |
| `RULE-OT-045` | Columna HERRAMENTAL en Plan por operador para doblado con `SIN_CT`: cascada `op.herramental` → `effectiveJobTool` (config de la OT antes del early-return de ct) → `toolCatalogForAppOperation` si `isBendingAppOperation`; el feed trae ct vacío y el código solo en `descripcion` | IMPLEMENTADA | bug usuario 2026-09-23; `app.js` (`renderProductionReportRow`), `planning-workflow-core.js` (`effectiveJobTool`); tests |
| `RULE-REP-015-A` | Un fallo de lectura de inspección queda registrado con folio, método y motivo real (`inspectionWorkOrderFailures` + `console.warn` + un solo `showToast` agregado) y el status crudo de NetSuite se escribe en el registro de ejecuciones (`console.error` en `PP_Inspection_restlet_`); el puente resuelve `{ok:false}` sin rechazar, así que ese camino ya no se traga en silencio | IMPLEMENTADA | bug usuario 2026-09-25 (`NetSuite inspeccion: 400` = `SSS_REQUEST_LIMIT_EXCEEDED` transitorio, RULE-OT-046); `app.js` (`recordInspectionWorkOrderFailure`, `inspectionWorkOrderFailureLines`, `ensureInspectionWorkOrders`), `16-inspection-service.js` (`PP_Inspection_restlet_`), `Bridge.html`; tests inspection-service + inspection-fill-observability; `.project-memory/rules.json` RULE-REP-015-A |
| `RULE-OT-048` | Una OT que NetSuite cerró sale de `CONFIG.selectedOts` en el servidor, no solo de la memoria del cliente: `PP_applyNetSuiteWorkOrdersData_` poda `selectedOts`/`lockedOts`/`expandedOts`/`lastSchedule.scheduledOts` contra el catálogo abierto, `PP_writeNetSuiteWorkOrdersState_` las persiste y `syncNetSuiteWorkOrdersLite` devuelve la cola ya sincronizada. Además una carga fallida avisa con toast y no solo con el banner | IMPLEMENTADA | bug usuario 2026-09-25 (OT 3483 cerrada en NetSuite conservaba su tarjeta en Planeado/No planeado); `08-netsuite.js`, `02-storage.js`, `15-performance-service.js`, `app.js` (`applyNetSuiteWorkOrdersPayload`, `pruneDraftToOpenWorkOrders`, `renderPriorityQueue`, `loadAppStateInBackground`, `syncNetSuiteData`), `build-appscript.mjs`; tests netsuite-operation-catalog + boot-sync-observability; `.project-memory/rules.json` RULE-OT-048 |
| `RULE-OT-049` | `completeAll` nunca aborta por presupuesto de tiempo: el flag se registra en `createPlanningPerformanceState`, no solo en `schedulePlanOnce`, porque el guard de presupuesto de `schedulePlan` corre sobre el estado compartido antes de la primera estrategia; el presupuesto sigue mandando cuando no es `completeAll` | IMPLEMENTADA | bug encontrado por el workflow de Pages (pass 640 fail 1 en CI, 641/641 en local) al publicar RULE-OT-048; `planner-core.js` (`createPlanningPerformanceState`, `checkPlanningBudget`, `schedulePlan:232`, `schedulePlanOnce:308`, rama de `completeAll` en 105), `app.js:5595-5606` (`completeAll: true` en Generar plan); tests planner-core (2 nuevos) |
| `RULE-OT-050` | La carga de OTs también reconcilia el borrador: `applyNetSuiteWorkOrdersPayload` aplica `reconcileActiveWorkOrders` antes de sustituir `workOrders`, así que una OT cerrada sale de `operations`, estados, materiales, configuraciones y `lastSchedule`, deja su resumen `CERRADA` y deja de aparecer en "trabajos en espera" | IMPLEMENTADA | ajuste de RULE-OT-048 a petición del usuario 2026-09-25; `app.js` (`applyNetSuiteWorkOrdersPayload`, `getPriorityJobs:11676`, `renderPriorityList:2261`, `jobStatusFromOperations:12458`), `planning-workflow-core.js` (`reconcileActiveWorkOrders`), `02-storage.js:373-374` (la carga no escribe `OPERACIONES`); tests performance-client-calls (2 nuevos con el core real) |

## OT

| ID | Nombre | Estado | Fuente |
|---|---|---|---|
| `RULE-OT-001` | Mapeo de hojas (Control → Trabajos → Operaciones) | DOCUMENTADA | `OT_RULES.md` |
| `RULE-OT-002` | Normalización de TRABAJO/Folio | DOCUMENTADA | `OT_RULES.md` |
| `RULE-OT-003` | ID Interno como cadena | DOCUMENTADA | `OT_RULES.md` |
| `RULE-OT-004` | Agrupación por secuencia | DOCUMENTADA | `OT_RULES.md` |
| `RULE-OT-005` | Coherencia del borrador (`selectedOts`) | DOCUMENTADA | spec 2026-07-12 |
| `RULE-OT-006` | Sincronización ligera del backlog | IMPLEMENTADA | `planning/app.js` |
| `RULE-OT-007` | Exclusión mutua de sync/programación (2026-09-25: Generar/Publicar plan dispara sync ligera interna si `syncedAt` ≥15 min y aborta si hay sync en curso — RULE-OT-046) | IMPLEMENTADA | task-2-report |
| `RULE-OT-008` | OTs cerradas conservadas (`CLOSED_KEPT`) | IMPLEMENTADA | task-2-report |
| `RULE-OT-009` | Tipos de trabajo (PROTOTIPO/URGENTE/EXPEDITACION) | IMPLEMENTADA | `legacy/IndexPlanning.html` |
| `RULE-OT-010` | Persistencia de `CONFIGURACION_OT` mientras OT abierta | DOCUMENTADA | definición de usuario 2026-08-12 |
| `RULE-OT-011` | Inicio del borrador: arranque desde la hora de ejecución con semana = lunes; CORTE 2026-09-22: en `lockedOts` sólo COMPLETADAS/históricas anclan, incompletas se reprograman; 2026-09-23: incompletas con inicio completo conservan ancla (RULE-BAL-025) | IMPLEMENTADA | definición de usuario 2026-08-13; CORTE 2026-09-22; `.project-memory/rules.json` RULE-OT-042/RULE-BAL-025 |
| `RULE-OT-012` | Precedencia por secuencia incluida; CORTE 2026-09-22: corte de fijas = COMPLETADA, incompletas de locked se reprograman | IMPLEMENTADA | definición de usuario 2026-08-13; CORTE 2026-09-22 |
| `RULE-OT-013` | Vigencia por OT de datos de planeacion (24h) y fallback parcial | IMPLEMENTADA | definición de usuario 2026-08-13, 2026-08-27 |
| `RULE-OT-014` | Sincronización inteligente y actualización por OT | IMPLEMENTADA | definición de usuario 2026-08-14 |
| `RULE-OT-015` | OT u operación nueva no inicia antes de la hora actual | IMPLEMENTADA | definición de usuario 2026-08-14 |
| `RULE-OT-016` | Dry-run de rendimiento de planeación | IMPLEMENTADA | definición de usuario 2026-08-26 |
| `RULE-OT-017` | `prepareDraftForReschedule` conserva ancla (fechas+operador) de incompletas seleccionadas con inicio completo (fecha+hora) y limpia sólo las sin inicio completo; completadas/históricas/no seleccionadas intactas | IMPLEMENTADA | definición de usuario 2026-08-27; CORTE 2026-09-22; decisión usuario 2026-09-23 (ancla); `.project-memory/rules.json` RULE-OT-017/RULE-BAL-025 |
| `RULE-OT-039` | `applyNetSuitePlanningPayload` hace merge inline por OT de las rutas frescas de NetSuite en OTs seleccionadas (sin descartarlas); bloqueadas (`lockedOts`) conservan local; timeout `syncNetSuitePlanningData` 360 s. Dry-run 2026-09-22: CORTADOR INICIAL 24→119 ops, corte 20→139 OTs, `plannerElapsedMs` 66754→23715 | IMPLEMENTADA | `app.js` (`applyNetSuitePlanningPayload`), `apps-script-bridge-client.js`, `probe_dryrun_local_after_fix.mjs`, `.project-memory/rules.json` |
| `RULE-OT-040` | Espera del cliente en `syncNetSuitePlanningData` = `NETSUITE_PLANNING_TIMEOUT_MS * 24` (360 s) alineada con `METHOD_TIMEOUT_MS` (antes `*4` = 60 s → `partial`) | IMPLEMENTADA | definición/fix 2026-09-22; `app.js:8604`; `.project-memory/rules.json` |
| `RULE-OT-041` | 6 OTs 2027 con `lockedOts` residual (3413/3416/3529/3533/2613/3398); limpieza vía `scripts/depurar-ots-2027.gs` (dry-run por defecto, no ejecutado) | DOCUMENTADA | análisis 2026-09-22; `scripts/depurar-ots-2027.gs`; `.project-memory/rules.json` |
| `RULE-OT-042` | Corte de anclas = COMPLETADA (no `lockedOts`); ops incompletas de OT locked se reprograman sin desbloqueo (conservan ancla de fecha+hora, RULE-BAL-025); completadas NO reservan capacidad (CORTE 2026-09-23) | IMPLEMENTADA | decisión usuario Lote 2 2026-09-22 + 2026-09-23; `planner-core.js`/`planning-workflow-core.js`; A/B `probe-lotes-a-b.mjs`; `.project-memory/rules.json` |
| `RULE-OT-043` | Generar plan: si alguna OT apta queda sin ops tras el sync → 1× retry (`force:true`); si aún falta → **bloquea** (sin plan parcial; elimina toast “Plan parcial…”) | IMPLEMENTADA | decisión usuario 2026-09-23; `app.js` `scheduleCurrentPlanImpl`; `tests/build.test.mjs` |
| `RULE-OT-046` | Gate de frescura antes de generar/publicar: si `state.syncedAt` tiene ≥15 min (`NETSUITE_WORKORDER_FRESH_MS`) se dispara la sync ligera de OTs (`onlyOpen:true`) y se retiran las OTs cerradas de NetSuite; si falla o hay sync en curso → aborta. Sin red si ya sincronizaste hace <15 min; 2026-09-25: la sync ligera reintenta 1× ante `400 SSS_REQUEST_LIMIT_EXCEEDED` (esperas 2/5/10 s en `PP_netSuiteRestletRequest_`, +5 s en cliente, timeout 110 s) y el aviso de aborto muestra el motivo real | IMPLEMENTADA | problema usuario 2026-09-24; `app.js` (`ensureNetSuiteWorkOrdersFresh`, `scheduleCurrentPlanImpl`, `publishCurrentPlan`), `planning-workflow-core.js` (`needsWorkOrderSyncBeforeSchedule`), `08-netsuite.js` (`PP_netSuiteRestletRequest_`); tests workflow-core/build/performance-client-calls/netsuite-operation-catalog; `.project-memory/rules.json` RULE-OT-046 |
| `RULE-OT-047` | El traslado Backlog → Planeado/Por planear se persiste de inmediato y se acusa (`await flushPlanSave`); el servidor es la autoridad de `selectedOts` en cada recarga, el cache local conserva `selectedOts`/`lockedOts`, ninguna carga remota borra un alta no guardada y ningún ámbito sucio se descarta en silencio | IMPLEMENTADA | bug usuario 2026-09-25; `app.js` (`flushPlanSave`, `performSelectJob`, `selectJob`), `performance-client.js` (`compactLocalState`, `optimizedSaveAppSheet`, `loadInitialStateConditionally`, `flushPendingSaveOnUnload`); tests performance-client-calls/conflict/build; `.project-memory/rules.json` RULE-OT-047 (deriva de RULE-GOV-013) |

Nota `RULE-OT-014`: la ruta directa de OT (`getPlanningWorkOrderData`) entrega cada operación con
`cantTotal`/`cantPendiente` = cantidad pendiente real (`Cantidad` − `Cantidad ensamblada`) vía
`'Cantidad a procesar'` en la fila; coherente con `tiempoProd`.

## Doblado

| ID | Nombre | Estado | Fuente |
|---|---|---|---|
| `RULE-DOB-001` | Precarga CT 5459/5527 en preparación de OT | DOCUMENTADA | spec 2026-07-12 |

## Herramentales

| ID | Nombre | Estado | Fuente |
|---|---|---|---|
| `RULE-HER-001` | Preservación de `HERRAMENTALES` | DOCUMENTADA | spec 2026-07-12 |
| `RULE-HER-002` | Herramental en doblado (complemento DOB-001) | INFERIDA | spec 2026-07-12 |
| `RULE-HER-003` | Múltiples herramentales simultáneos en doblado | IMPLEMENTADA | definición de usuario 2026-08-12 |
| `RULE-HER-005` | Selector de herramental del panel de OT sin filtro por artículo (unión de activos del catálogo) | IMPLEMENTADA | bug 2026-09-06, `app.js` |

## Máquinas

| ID | Nombre | Estado | Fuente |
|---|---|---|---|
| `RULE-MAQ-001` | Preservación de `MAQUINAS` | DOCUMENTADA | spec 2026-07-12 |
| `RULE-MAQ-002` | Programa semanal = vista Gantt por máquina | IMPLEMENTADA | `legacy/IndexPlanning.html` |
| `RULE-MAQ-003` | Cada operación tiene máquina asignada | IMPLEMENTADA | `legacy/IndexPlanning.html` |

## Matriz de capacidad

| ID | Nombre | Estado | Fuente |
|---|---|---|---|
| `RULE-MAT-001` | Catálogo maestro SuiteQL | DOCUMENTADA | spec 2026-07-26 |
| `RULE-MAT-002` | Dedupe y filtro de activos | DOCUMENTADA | spec 2026-07-26 |
| `RULE-MAT-003` | Fallback del catálogo (nunca lista vacía) | DOCUMENTADA | spec 2026-07-26 |
| `RULE-MAT-004` | No modificar RESTlets/Apps Script | DOCUMENTADA | spec 2026-07-26 |
| `RULE-MAT-005` | Exclusión de subcontratos especiales | DOCUMENTADA | spec 2026-07-26 |
| `RULE-MAT-006` | Buscador de operaciones | DOCUMENTADA | spec 2026-07-26 |
| `RULE-MAT-007` | Exclusiones globales (`excludedCapabilities`) | DOCUMENTADA | spec 2026-07-26 |
| `RULE-MAT-008` | Memorización de operaciones incluidas | IMPLEMENTADA | spec 2026-07-27 |
| `RULE-MAT-009` | Catálogo con caché 1 h | IMPLEMENTADA | spec 2026-07-27 |
| `RULE-MAT-010` | Edición local de capacidades sobrevive al import remoto | IMPLEMENTADA | bug 2026-09-19 |
| `RULE-MAT-011` | Operaciones sin operador resaltadas en rojo tenue | IMPLEMENTADA | decisión 2026-09-23 |
| `RULE-MAT-012` | Vista skills comparte el mismo almacén Google Sheets | IMPLEMENTADA | decisión 2026-09-23 |

## Balanceo

| ID | Nombre | Estado | Fuente |
|---|---|---|---|
| `RULE-BAL-001` | KPIs de carga | IMPLEMENTADA | `legacy/IndexPlanning.html` |
| `RULE-BAL-002` | Cargas coherentes con `selectedOts` (misma regla OT-005) | DOCUMENTADA | spec 2026-07-12 |
| `RULE-BAL-003` | Agrupación por máquina/herramental/kit con trade-off controlado de setup vs entrega | DOCUMENTADA | definición de usuario 2026-08-12, 2026-08-14 y 2026-08-29 |
| `RULE-BAL-004` | Menor tiempo de OT en producción para reducir WIP | DOCUMENTADA | definición de usuario 2026-08-12 |
| `RULE-BAL-005` | Validación de operador en operaciones sincronizadas de NetSuite | IMPLEMENTADA | definición de usuario 2026-08-27; `08-netsuite.js` |
| `RULE-BAL-006` | Operador programado siempre desde la matriz de habilidades (MATRIZ), nunca residual; CORTE 2026-09-22: `OPERADOR_REASSIGN_NO_MOVE` en `appendLog`; empates `toolAffinityTie` + menor carga | IMPLEMENTADA | definición de usuario 2026-08-27; CORTE 2026-09-22; `planner-core.js`/`app.js` |
| `RULE-BAL-007` | Validación previa de operador en la matriz antes de generar plan | IMPLEMENTADA | definición de usuario 2026-08-27; `app.js` `planner-core.js` |
| `RULE-BAL-008` | Arranque por orden de Planeado/No planeado; sucesoras optimizables y terminación flexible | IMPLEMENTADA | definición de usuario 2026-08-29; `planner-core.js` |
| `RULE-BAL-009` | Modo rápido 5 min con keep-best-on-timeout y estrategias acotadas | IMPLEMENTADA | definición de usuario 2026-08-29; `app.js` `planner-core.js` |
| `RULE-BAL-017` | Reorden de Planeado/No planeado cruza trabajos fijos; OT bloqueada no se mueve como origen; incompletas de locked se reprograman (CORTE 2026-09-22) | IMPLEMENTADA | definición de usuario 2026-09-06; CORTE 2026-09-22; `app.js` |
| `RULE-BAL-015` | El ancla de hora de ejecución solo aplica al MISMO día; los días siguientes respetan el calendario (07:00) | IMPLEMENTADA | definición de usuario 2026-09-04; `planner-core.js` `computeEarliestStart`; afina `RULE-OT-015` |
| `RULE-BAL-016` | La escala temporal del Gantt usa el horario real por día (workSchedule/dailyBreaks/calendarExceptions), con columnas de día proporcionales a sus minutos laborables | IMPLEMENTADA | definición de usuario 2026-09-04; `app.js` `renderGantt`/`workWindowMinutes`/`workMinuteOffset`/`dateFromWorkOffset` rework multi-ventana por día; `styles.css` flex con columnas proporcionales; afina `RULE-BAL-015` (escala) |
| `RULE-BAL-022` | Dry-run 2026-09-22: se mantiene `strategyPool` default `[balanced_goal]` y NO se cambian pesos de `evaluatePlan` (decisión de usuario); `balanced_goal` gana monto semana 1; pool doble sin ganancia; `fastQualityMode` anula ramas de estrategia en sucesoras (`planner-core.js:2128`) | DOCUMENTADA | decisión usuario 2026-09-22; `probe_strategy_compare.mjs`/`probe_monto_compare.mjs`; `.project-memory/rules.json` |
| `RULE-BAL-023` | Empates de asignación: `windowedTieBreak` con ventana 20 min (tool-affinity no-cambio=2/kit=1/cambio=0 → menor `operatorIdleCost`) + `selectTopKAssignment` top-3; en `compareFirstOperationCandidates` va después de `end`/`fechaReq` (no pisa arranque), al frente en assign/interleaved y tras `gapFit` en flow; A/B: solo A ≡ baseline, A+B `toolChanges` 24→23 `idle` 30683→28600; pesos `evaluatePlan` intactos | IMPLEMENTADA | decisión usuario Lote 1 2026-09-22 + Lote 3 2026-09-23; `planner-core.js`; A/B `probe-lotes-a-b.mjs`; `.project-memory/rules.json` |
| `RULE-BAL-024` | Gap-fill post-schedule (opción C): flag `gapFill {enabled, minGapMinutes=60, maxCandidates=50, budgetMs=3000}` tras el loop principal; huecos ≥ minGap por operador, mueve ops sin fixed ni tool key (no completadas/fijas/históricas), respeta precedencia/sucesores, budget propio no aborta el plan; A/B 2026-09-23: control OFF `49c3f05b` idle 28428 avoidable 5146 → C max50 `59819d93` idle 27930 (−498) avoidable **5268 (+122)** `toolChanges` 24→24 wall +1.0 s; **criterio no cumplido (avoidable sube) → default OFF** | DOCUMENTADA | dry-run Lote 4 2026-09-23; `planner-core.js:28,514,2671,2905`; `probe-lotes-a-b.mjs` `AB_GAPFILL`; `.project-memory/rules.json` |
| `RULE-BAL-025` | Ancla de fecha (2026-09-23): `prepareDraftForReschedule` conserva fechas+operador de ops incompletas seleccionadas con inicio completo (fecha+hora), limpia sólo las sin inicio; motor adelanta si hay hueco antes de la ancla o la conserva si no (forward scheduling, sin pin); jobs con `hasAnchor` ganan el primer hueco sobre OTs nuevas (tier en `compareReadyCandidates` antes de `orderIndex`); operador previo preferido si está en el `minStart`, si no gana el de `minStart` con operador disponible (`selectOperationAssignment`); `commitAssignment` limpia `autoFrozen`; gap-fill no adaptado | IMPLEMENTADA | decisión usuario 2026-09-23; `planning-workflow-core.js:126`; `planner-core.js` `buildJobs`/`compareReadyCandidates`/`selectOperationAssignment`; `tests/planner-core.test.mjs` 6 tests; `.project-memory/rules.json` |

## BOM

| ID | Nombre | Estado | Fuente |
|---|---|---|---|
| `RULE-BOM-001` | `MATERIALES` en limpieza del borrador | DOCUMENTADA | spec 2026-07-12 |
| `RULE-BOM-002` | `materialsForOt()` filtra por OT | IMPLEMENTADA | `CONFIGURACION.md` |

## Calendario

| ID | Nombre | Estado | Fuente |
|---|---|---|---|
| `RULE-CAL-001` | Turno y ventanas de trabajo por defecto 07:00–17:00 | IMPLEMENTADA | `planner-core.js`, `app.js`, `02-storage.js` |
| `RULE-CAL-002` | Partición de operaciones entre días laborables consecutivos | IMPLEMENTADA | definición de usuario 2026-08-16; `planner-core.js`; `tests/planner-core.test.mjs` |

## Montos

| ID | Nombre | Estado | Fuente |
|---|---|---|---|
| `RULE-MON-001` | Monto de liberación; PRECIO = max(ops, última/promedio, manual); piso $1 MXN (candidatos < $1 = no registrado) | IMPLEMENTADA | usuario 2026-08-17; max precio 2026-09-23/24; piso $1 2026-09-24; `app.js` `weeklyJobSummary`/`operationToRow`/`effectiveUnitPriceForOt`; `tests/weekly-summary.test.mjs`; `tests/build.test.mjs` |
| `RULE-MON-002` | Preserva `unitPrice`/`amount` de operaciones al importar o fusionar | IMPLEMENTADA | bug 2026-09-24 (montos Plan semana en $0 tras tiempo); `app.js` `preserveImportedOperationPrices`/`applyImported`/`mergeIndividualPlanningData`; `tests/performance-client-calls.test.mjs`; `tests/build.test.mjs` |
| `RULE-FIN-001` | Precios de venta desde Restlet 1766 REQ_FIFO (max última/promedio 6m); match por `_ITEM_ID` **o `PARTE`** (WO_LISTA sin `Item Internal ID`); modal solo si las 3 fuentes y la ops son 0/`<$1` (`ot_manual_price` min $1 MXN); cada sync sube `PRECIO_MANUAL` a `max(manual, last, avg)` ≥ $1 | IMPLEMENTADA | usuario 2026-09-23 (solo restlet 1766; refuerzo needsManualPrice); piso modal $1 2026-09-24; no pedir si ops ≥ $1 2026-09-24; FIX match `PARTE` 2026-09-24 (probe C 490 UADE PN/OT 3386 cerrada); persistencia de precio de referencia en sync 2026-09-24; `08-netsuite.js`; `02-storage.js`; `app.js` (`persistReferencePricesFromSync`); `tests/netsuite-operation-catalog.test.mjs`; `tests/performance-client-calls.test.mjs`; `tests/build.test.mjs` |

## Pendientes y ambigüedades

- Balanceo y BOM carecen de reglas explícitas más allá de lo listado (evidencia insuficiente).
- Los enlaces a líneas/archivos pueden quedar obsoletos; este registro referencia fuentes, no las reescribe.
