# plangit

Project Memory is the primary context layer for humans and AI agents.

## AI navigation contract

Before broad code search, read `.project-memory/indexes.json` and only retrieve the relevant rules, data sources, modules and architecture decisions.

Any knowledge discovered from code that will matter again must be written back into Project Memory.

## Documentación para reconstrucción desde cero

El índice maestro y el orden de lectura completo están en `docs/README.md`. El documento
maestro del sistema está en `docs/PLAN-MAESTRO.md` y la guía de reconstrucción en
`docs/REBUILD.md`. Ver también el mapa en `docs/README.md#mapa-de-documentos`.
