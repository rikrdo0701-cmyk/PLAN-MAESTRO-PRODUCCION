# Contributing

Every change must preserve Project Memory, pass tests, update affected rules/data/module docs, and pass the Git Gate before remote synchronization.

Use Conventional Commit style where practical.
